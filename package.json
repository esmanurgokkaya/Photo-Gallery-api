{
  "name": "photo-gallery-api",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "npm run prisma:generate && node server.js",
    "prisma:generate": "prisma generate",
    "postinstall": "npm run prisma:generate",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "type": "module",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.913.0",
    "@prisma/client": "^5.22.0",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "multer": "^2.0.2",
    "zod": "^4.0.17"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  }
}
